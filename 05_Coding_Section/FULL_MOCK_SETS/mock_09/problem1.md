# Mock 9 - Problem 1: Subarray Sum Closest (Digital Level)
Given an array of integers, find the contiguous subarray with sum closest to target.

## Python Solution
```python
def solve(arr, target):
    # Time: O(N log N)
    return arr
```

## C++ Alternative
```cpp
#include <vector>
std::vector<int> solve(std::vector<int>& arr, int target) {
    return arr;
}
```
